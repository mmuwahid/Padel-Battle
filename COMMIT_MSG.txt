[Session038] push notifications: in-app pref filter + stale cleanup + Test Push button + combined match push

Bug #1: Edge Function notifications insert now filters by user notification preferences
  (matches the push send filter). Users who disabled "New Match" will no longer
  see it in the bell either. Users with no subscription row still get in-app (default allow).

Bug #3: delete_stale_push_endpoints now receives a Postgres array instead of
  a JSON string. The previous JSON.stringify(arr) broke the RPC silently —
  stale endpoints accumulated forever (visible in DB: one user had 4 rows).

Bug #4: sendPushNotification surfaces {sent, failed, cleaned, total} in console
  for diagnostics. Returns the result instead of swallowing.

Bug #6: subscribeToPush now deletes the user's OTHER endpoints in this league
  before upserting the new one. Prevents stale endpoint accumulation across
  permission resets / reinstalls.

Bug #7: New Test Push button in Settings (visible when push is enabled) sends
  a self-targeted push and reports the response. Critical diagnostic for iOS PWA.

Bug #2: LogMatch now fires ONE push per match (was 2: "New Match" + "Rankings Updated")
  with winner + loser teams + set summary in the body, e.g.:
  "Basel & Chaos beat Moody & MAK (6-4, 6-3) — leaderboard updated"

Edge Function deployed as v17.
SW v36 -> v37.
