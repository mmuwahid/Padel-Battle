# Sessions Index — PadelHub

> **Convention:** `YYYY-MM-DD — Session### — Short Title.md`
> Session### is a global counter that never resets. Always increments.
> **Note:** Sessions S001–S015 used the old `S### — YYYY-MM-DD` naming. From Session016 onward, the new format applies. Old files are not renamed.

| # | Date | Title | Type | Phase | Key Deliverables |
|---|------|-------|------|-------|-----------------|
| S001 | 2026-03-27 | Stage 1 Bug Fixes | Build | P7-S1 | 14 bugs fixed (BF-01 to BF-14), git CLI deploy setup |
| S002 | 2026-03-27 | Stage 2-3 UX + Features | Build | P7-S2/S3 | All 19 GN items, FT-03 photo upload, FT-04 analytics, FT-05 scheduling, FT-06 brackets |
| S003 | 2026-03-27 | Stage 3 Features + Fixes | Build | P7-S3 | Sidebar restructure, ErrorBoundary, analytics/scheduling polish |
| S004 | 2026-03-27 | Stage 4 Bug Fixes (BF-16 to BF-21) | Build | P7-S4 | 6 supplementary bugs, toast notch fix, PWA icons regenerated |
| S005 | 2026-03-27 | FT-02 App.jsx Refactor | Build | P7-S5 | 3,539 → 1,572 lines, 19 files, pre-refactor archived |

| S006 | 2026-03-28 | FT-01 Push Notifications | Build | P7-S6 | VAPID keys, push_subscriptions table, sw.js push handlers, Edge Function, notch fixes |

| S007 | 2026-03-28 | GN-21 Scheduling Flow + Cleanup | Build | P7-S7 | Challenge push, leave/mark-played, upcoming/past tabs, notif cleanup, workspace reorg |

| S008 | 2026-03-28 | Footer Fix + Tournament Mockups | Build/Plan | Post-P7 | Footer icon alignment, horizontal court icon, tournament V2 mockups approved |

| S009 | 2026-03-28 | BF-22 Log Match + Schedule Fixes | Build | Post-P7 | Inline score entry from schedule, RLS fix, cancelled filter, schema updates |

| S010 | 2026-03-28 | Tournament Mode V2 | Build | Post-P7 | Full GameMode rewrite: SE (SVG bracket), DE (winners+losers+grand final), RR (group table+rounds). 285→1457 lines |

| S011 | 2026-03-28 | BF-24 BF-23 BF-16 + Multi-PC Sync | Build/Fix | Post-P7 | DB migration (columns+CHECK), player dedup, OG image fix, sw.js cache bust v4, local folder sync, stale data cleanup |

| S012 | 2026-03-28 | UX + Performance Audits | Audit | Post-P7 | 26 UX findings, 10 perf fixes mapped, 1.5MB dead assets found, bundle analysis (584KB JS). No code changes — plan only. |

| S013 | 2026-03-28 | Performance + UX Sprint | Build | Post-P7 | 20 fixes (10 perf + 10 UX). Bundle: 584KB monolith → 6 chunks (no warning). Promise.all, React.lazy, vendor splitting, O(n^2) ELO fix, dead asset cleanup. UX: inline confirmations, ARIA, numeric keypad, contrast fix, nicknames, active tab. Commit: 33cafa3 |

| S014 | 2026-03-29 | H2H Migration + Icon Health Check + Admin Fix | Build/Fix | Post-P7 | BF-13 deferred. BF-23: H2H→Players tab. BF-24: Icon dedup. BF-25: Dead sidebar button. BF-26: H2H prop fix. Admin blank screen: missing JSX braces + missing useState (S013 regression, 2 commits to fix). 4 new lessons. Commits: 530f9f4→9c70a6a→de2e9a5→4c724a2 |

| S015 | 2026-03-29 | Codebase Audit + Implementation Plan | Audit/Plan | Post-P7 | 4-phase audit (UX B-, Perf B+, Arch C+, Overall B-). 37 findings. S016-S019 implementation plan created. No code changes. |

| S016 | 2026-03-29 | Quick Wins + UX Polish | Build | Post-P7 | 13 fixes: console cleanup, fm→matches rename, LeagueGate toast+double-submit, sidebar X/icons/tap, admin loading, ELO labels, step indicator, scroll hint, sw v6. Commit: 0fd50d7 |

| S017 | 2026-03-30 | Full UI Review + LeagueGate Fixes | Build/Fix | Post-P7 | Full 16-view UI walkthrough, BF-27 sign-out on LeagueGate, BF-28 hide invite from non-admins, sw v7. Commit: c6067ff |

| S018 | 2026-03-30 | Form UX + Accessibility | Build | Post-P7 | 7 fixes: schedule form persistence, focus rings, skeleton loading, H2H ELO badges, season scroll hint, v2.0 removed, Outfit 500 trimmed. Commit: d12ce47 |

| S019 | 2026-04-01 | App.jsx Extraction + Audit + S020 Plan | Build/Plan | Post-P7 | 4 components extracted (App.jsx 1,577→1,068). Deferred items audit (19 sessions). 4 user verifications confirmed. BF-13 partial, BF-15 closed. Vercel+gcloud CLI installed. S020 plan written+approved. Commits: 9607fe0, 27071d8 |

| S020 | 2026-04-01 | GameMode Refactor + Notifications + Reactions | Build | Post-P7 | Phase 1: GameMode 1,450→115 lines (5 components). Phase 2: notifications + match_reactions tables, Edge Function rewrite, notif_challenges bug fix. Phase 3: NotificationCenter UI, match reactions, rank change + new members push triggers, auto-expiry. Edge Function debugged (RLS→RPC). Commits: 17ca30e, 180f2ac, feccda4 |

| S021 | 2026-04-01 | Challenge Confirmation Flow + Optimization | Build | Post-P7 | Challenge pending/accept/decline flow with response indicators. A-09: SELECT * → specific columns (8 queries). P-12: challenges + notifications Realtime. U-16: Delete Tournament with inline confirm. Commits: 0d44007, 88cd7a3 |

| S022 | 2026-04-01 | React Context + Notification Polish + Critical Bugfix | Build/Fix | Post-P7 | A-04 LeagueContext. GN-21 all 4 notif toggles. Challenge expiry/cancel/play notifications. CRITICAL: A-09 column selects used nonexistent columns (players.active, profiles.user_metadata) → 400 errors → all data appeared deleted. 2 hotfixes. BF-13/GN-20 closed, Phase 8+/9+ cancelled. Commits: 31ce829, 06e3fdc, b71588f |

| S023 | 2026-04-01 | Error Toast + useLeague Migration | Build | Post-P7 | Error toast on data load failure (prevents silent S022-type incidents). useLeague() migration: MatchHistory, AdminDashboard, GameMode — 18 props eliminated. Commit: 3812664 |

| S024 | 2026-04-01 | Full Codebase Audit & Adversarial Review | Audit | Post-P7 | Triple-layer review (code reviewer + Codex adversarial + deep explore). 10 critical, 10 high, 12 medium findings. Grade: B-. 4-phase fix plan created. 3 new bugs (BF-29/30/31). No code changes. |

| S025 | 2026-04-01 | Security Hardening + Runtime Fixes | Build/Fix | Post-P7 | BF-29/30/31/32 fixed. RLS hardened (6 policy changes, 4 table policies, invite RPC). Player profile blank screen fixed (Rules of Hooks). 2 commits: 7325a4e, 922bffe. |

| S026 | 2026-04-02 | Data Integrity RPCs + Performance | Build | Post-P7 | 3 atomic RPCs (respond_to_challenge, join_challenge, play_challenge). ErrorBoundary on lazy components. Debounced realtime. ELO floor. Matches limit(500). LeagueContext useMemo confirmed impossible. Commit: 8c03dc3. |

| S027 | 2026-04-02 | Invite + League Creation Fixes | Build/Fix | Post-P7 | BF-33: invite link preserved through OAuth/signup/reset redirects. BF-34: leagues_select RLS creator fallback. ELO recalc confirmed working. Platform Admin designed for S028. Commit: 3b04eb6. |

| S028 | 2026-04-02 | Platform Admin + S026 Backlog | Build | Post-P7 | PlatformAdmin.jsx (170 lines). 5 platform RPCs. Verified on production. S026 backlog finalized: DB CHECK constraints, tournament version col, push JWT verification, useLeague closed. Commits: 56060d1, 1f732b0. |

| S029 | 2026-04-02 | Web Push Delivery Fix | Build/Fix | Post-P7 | RFC 8291 encryption, VAPID ES256, targeted notifications, in-app notifications, deep linking, security hardening. 4 commits: efe69d1→574d9a0. |

| S030 | 2026-04-03 | Security Hardening + Performance | Build/Audit | Post-P7 | H-2 CORS, H-5 leave_challenge RPC, L-4 parallel push, M-3 rate limiting. Triple-layer audit: BF-35 cross-league delete, BF-36 display_name, BF-37 logged_by. Leaderboard click-to-profile. Commits: 6b4327d, cd9ee56. |
| S031 | 2026-04-03 | Leaderboard Ranking Fix | Build/Fix | Post-P7 | BF-38: Leaderboard sort rewritten (Win Rate > ELO > Games Played). "Ranked by Win Rate" label. Commit: b6367a5. |
| S032 | 2026-04-05 | Auth Login Loop Fix | Build/Fix | Post-P7 | BF-28: Password recovery form, friendlyAuthError() mapper, resend confirmation, stale session cleanup. Commit: e1e3d85. |
| S033 | 2026-04-05 | Blank Screen PWA Fix | Build/Fix | Post-P7 | SW network-first for hashed assets, branded HTML splash screen, auth 5s timeout, SW auto-reload on update. Commit: 57839ac. |
| S034 | 2026-04-09 | Leaderboard Total Wins Ranking | Fix | Post-P7 | Sort changed to Total Wins > Win Rate > ELO > Games Played. Podium/table display overhauled — WR prominent, ELO demoted. Commit: 0b9679b. |
| S035 | 2026-04-17 | FT-08 RNG Team Shuffler | Feature | Post-P7 | New shuffle.js + TeamShuffler.jsx (reusable pool picker + results). LogMatch multi-match queue, ScheduleView singleMatchMode. Fisher-Yates, any-member access, one-roll accept. FT-07 plan approved (deferred). SW v31→v32. Commit: 0cd5b85. |
| S036 | 2026-04-21 | Combos + Leaderboard Bug Fixes | Fix | Post-P7 | Bug 1: leaderboard wins conditional color (podium + rows). Bug 2: "Best Partner" label honest to data (Only Partner / Partner fallbacks). Bug 3: partner ranking tiebreaker pct→games, independent worst sort, hide when tied. Deploy incident: Vercel rejected commits from UNEC email on private repo → repo made public + git-author rule pinned in CLAUDE.md. Commits: 27be785, 72f11c4, 88a9141, 92314b8. |
| S037 | 2026-04-21 | League Activity Month Labels | Fix | Post-P7 | PlayerStats monthly trend bars now show `Mar`/`Apr` via `toLocaleString("en",{month:"short"})` instead of `03`/`04`. Multi-PC desync discovered on push (remote ahead by S036) — rebased, pushed, then full tracked-files sync /tmp → OneDrive to bring local into parity. Commit: cfa9999. |
| S038 | 2026-04-29 | GitHub Issues #1-5 Analytics Fixes | Fix | Post-P7 | #1 Games Diff → Match Diff (W-L). First attempt (fa6587d) summed game scores, gave Luke -10; hotfix a570c9a switched to match-count W-L per user clarification. #2 League Activity proper x/y axes. #3 Partners formula matches Best Duos (pct DESC, games DESC tiebreaker, worst hidden when <6 partnerships). #4 H2H opponents shows explicit `<P1Name> won X / <P2Name> won Y`. #5 Biggest Wins shows winner-vs-loser teams + colored scores. #6 Push notifications deferred (needs iPhone live test). User confirmed #1 fix on prod. SW v32→v34. Commits: fa6587d, a570c9a. |

| S038b | 2026-04-29 | Issue #6 Push Notifications | Fix | Post-P7 | Systematic-debug audit found 6 real bugs: (A) in-app notifications ignored prefs → spammed bell, (B) `delete_stale_push_endpoints` got JSON string instead of array → cleanup never ran, (C) push response swallowed, (D) no on-device diagnostic, (E) endpoints accumulated on re-subscribe, (F) double match push (match+ranking). Edge Function v17 deployed, frontend 8da3f35, SW v37. Test Push button verified delivery on iPhone PWA — notifications arrive on home screen + bell dropdown. Issue #6 closed. |
| S039 | 2026-04-29 | Codebase Cleanup Pass | Build | Post-P7 | 7-track cleanup (Tracks 2 & 5 N/A — JS codebase). Lint baseline 81→39 problems (-42 errors). Track 1: `setTotals(sets)` helper consolidates 5 reduce sites. Track 3: `gid`, `ES` re-export, `actionLoading` state, `data:matchId` destructure removed. Track 4: zero circular deps confirmed via madge. Track 6: 14 silent DB-action catches now surface via showToast (PlayerStats, GameMode + 4 tournament components, NotificationCenter, App.unsubscribeFromPush); deletePlayer atomic-safe. Track 7: `requestNotificationPermission` (12 lines), unused tournament-utility imports, `failed`/`data` dead destructures removed. Live-verified end-to-end signed in to production league — Leaderboard, 6 match totals, Players Roster + Insights Biggest Wins, GameMode Casual+Competitive, Notifications, Settings — zero console errors. Bug confirmed live: "← Back to Leagues" on player-claim does nothing (deferred). devDeps: madge, knip. Findings report: `_wip/cleanup-findings-2026-04-29.md` (5 MEDIUM + 3 LOW). Commits: a2da5a3, d3c055d, 834fee8, aea36c8 (cleanup branch FF-merged), 349c8d5 (SW v37→v38). Deploy: dpl_CiFe1ZxpnRcKyPXyF4Nkaw1JuHAr READY. |
| S040 | 2026-05-02 | Score Input UX Fix | Fix | Post-P7 | All 8 set-score inputs across 6 components fixed: `type="number"` → `type="text" inputMode="numeric" pattern="[0-9]*"` restores iOS cursor placement + backspace; forced `onFocus.select()` removed from LogMatch/ScheduleView/RoundRobin/AmericanoMode; controlled inputs use `value={n===0?"":String(n)}` + digit-strip onChange + clamp (7 / ptsPerRound / no cap). SE/DE uncontrolled inputs: type-only change. Initial plan to extract a `ScoreInput.jsx` stepper component caught by advisor as scope creep → reduced to minimal in-place fix per CLAUDE.md workflow rules. SW v38→v39. Commit: fdc8ea8. Deploy: dpl_8h84QWuvLNMqpNgEUzUNh7QrrLjp READY. |
| S040b | 2026-05-02 | LIVE Scoring Mode Added (undocumented) | Build | Post-P7 | Two commits without session log: efed08d added `src/utils/scoringEngine.js` (full state machine: 0/15/30/40/Deuce/Ad, sets to 6 win-by-2, tiebreak at 6-6, history undo) + LIVE/Manual mode toggle in LogMatch.jsx with +1 tap zones, scoreboard, undo/reset, match-over banner; 77952d1 added 2/3 sets toggle. Bug introduced: engine hardcoded `matchOver` at first-to-2; wrapper flag couldn't extend match. Fixed in S041. |
| S041 | 2026-05-04 | Issue #7 LIVE Scoring Fix + Back to Leagues | Build/Fix | Post-P7 | GitHub issue #7 closed. scoringEngine parameterized `setsToPlay` through scorePoint chain — `matchOver = completedSets.length >= setsToPlay`. LogMatch: `formatTeam` import (canonical " x " separator) replaces 3× `' & '` joins; `scorePoint(s,'A',liveNs)` wires toggle through; winner banner rebuilt with 4 rows (emoji / winner-or-tie / sets count / per-set score) including 🤝 tie path for N=2 1-1 case. App.jsx: Back-to-Leagues fixed (`onSwitchLeague` instead of dead `setSelectedLeagueId(null)`); dead `selectedLeagueId` state dropped. SW v39→v40. Engine harness validated 3-sets+2-0 continues, setsToPlay=2 1-1 ends. Commit: ce5e7ea. Deploy: dpl_Aqy4rBp8HZHj4KeP3PQiZh6wF8iD READY. User confirmed live on iPhone. |
| S042 | 2026-05-04 | Cleanup Quick Wins | Build | Post-P7 | Three deferred items closed in one commit. App.jsx: dead `tournaments` state dropped (5 sites — declaration, fetch destructure, query, error throw, reset). One Supabase round-trip saved per `loadLeagueData()`. 6 push-diagnostic console.warn/log calls in `sendPushNotification` + `testPushNotification` gated behind `import.meta.env.DEV` (Vite tree-shakes from prod bundle). 14 unused `catch (err)` renamed to `catch (_err)` via brace-matched body scan across App.jsx (6), AdminDashboard (3), LeagueGate (2), LogMatch (1), MatchHistory (1), PlatformAdmin (1) — only catches with bodies that don't reference `err` were renamed. ESLint config: added `caughtErrorsIgnorePattern: '^_'` so the convention sticks. SW v40→v41. Browser-verified end-to-end on dev server: app loads, league select works, GameMode/Leaderboard/Matches/Combos/Players tabs all clean, zero console errors. Commit: 561a562. Deploy: dpl_8fgKpLVacfgyZnDKTD3EpxXEwDgB READY. |
| S043 | 2026-05-04 | Score Stepper + Team Colors | Feature/Build | Post-P7 | New `src/components/ScoreStepper.jsx` — reusable horizontal `[−][value][+]` stepper, 40×40 tap targets, theme-aware accent border, preserves S040 native iOS input behaviour (type/inputMode/pattern + digit-strip + clamp). Drop-in replacement for bare numeric inputs in 4 components: LogMatch (manual sets, max=7), ScheduleView (inline log form, max=7), RoundRobin (no cap), AmericanoMode (max=ptsPerRound, zero-sum preserved). SE/DE deferred to S044 — uncontrolled inputs need conversion to controlled state first. Plus: Team A/B colour change in LogMatch manual mode (A green → BL blue for Team A, DG red → GD yellow/gold for Team B) — resolves semantic collision with the app's win=green/loss=red palette. LIVE mode + ScheduleView + tournament components untouched (scope discipline). Mockup-first workflow followed: HTML mockup served via dev server, mobile-screenshot review, approval, then code. Two commits: 2ec43e4 (stepper), bf0e5a3 (colours). Deploys: dpl_EywhVVpWqCaWcu3Vyxrofniy8MG6 READY, dpl_77QiJUnZKFDwV1a5q6gBR7k6CTSE READY. SW v41→v42→v43. |
| S044 | 2026-05-05 | FT-09 Match Approval + Admin Promotion (+ hotfix + #9/#10) | Feature/Build/Fix | Post-P7 | All 3 open GitHub issues (#8/#9/#10) closed. Casual match submissions now go through admin approval; league owner can promote/demote members to admin. DB: `matches.status` column ('pending'\|'approved'), BEFORE INSERT trigger sets status via `is_league_admin_or_owner()`, AFTER INSERT trigger fans out admin notifications, RLS SELECT 3-way union, 4 RPCs (approve_match, reject_match, update_pending_match, set_member_role), `play_challenge` return type uuid → jsonb {id, status}. Six named sub-migrations applied. Frontend: `isOwner` split from `isAdmin`, `approvedMatches`/`pendingMatches` selectors at App.jsx top — all stats consumers read approved-only. **Approvals queue inline at top of Matches tab** (relocated from AdminDashboard via hotfix per user feedback). New `EditMatchModal.jsx` with live diff. MatchHistory adds "My Pending" section. AdminDashboard adds inline Promote/Demote (owner-only) with role badges (★ Owner / ⚡ Admin / You). NotificationCenter: 4 new variants with diff inset. AuthGate: password show/hide eye toggle (login + signup + recovery). SettingsView Admin Management gated to owner-only. **Hotfix round** caught: (a) admin notifications missing because no DB-side producer existed, (b) Promote button silently invisible because second `league_members.select()` missed `id` column, (c) queue-on-sidebar UX mismatch with user's natural discovery path. SW v43→v51. **Final round (commits e7b6a59, a78b537):** Promote button still missing on iPhone after closure round — diagnostic block surfaced root cause: `league_members_select` RLS policy `(user_id = auth.uid())` from S025 hardening returned only the user's own row; widened to `league_id IN (SELECT get_user_league_ids(auth.uid()))`. ALSO fixed challenge bug — `respond_to_challenge` + `join_challenge` confirmed challenges after only some players accepted because `bool_and` ignores NULL responses; replaced with explicit count comparison. One mistakenly-confirmed challenge backfilled to pending. **Polish round (commits b756a98, 5560e1a, a896849):** role-change UI purple→gold across 6 sites + push notification on promote/demote (was in-app only); "Remove" → "Delete" on player management; header v3 — content shrunk (logo 32→26, h1 16→14, subtitle 10→9, padding 8→4) since padding-only attempts were invisible. **Eight commits total** for S044: fb6b3c7, 4329493, 141c0aa, e7b6a59, a78b537, b756a98, 5560e1a, a896849. All deploys READY. iPhone-verified end-to-end. |

| S045 | 2026-05-06 | FT-09b FIP Scoring Enforcement | Feature/Build | Post-P7 | FT-09b shipped + iPhone-verified. Per-set FIP shape rejection (6-0..6-4, 7-5, 7-6 only), match completeness gate (2-set winner required for ranking), `incomplete` status enum value introduced for matches with valid sets but no 2-set winner. New `validateMatch` + `isValidSet` exports in `scoringEngine.js` (33/33 unit tests pass) — auto-truncates dead-rubber sets after 2-0, strips trailing [0,0], flags invalid shapes by index for red-border highlighting. Wired into LogMatch (manual + LIVE), ScheduleView inline log form, EditMatchModal (Save & Approve disabled until match is complete + valid). MatchHistory now merges approved + incomplete in timeline with grey tint + "Incomplete" badge + "not counted in rankings" caption. App.jsx `incompleteMatches` selector + context export. ScoreStepper gains `invalid` prop (red border). DB defense-in-depth: `is_valid_set(jsonb)` + `is_complete_match(jsonb)` + `all_sets_valid(jsonb)` IMMUTABLE helpers, status enum extended `pending\|approved\|incomplete`, BEFORE INSERT trigger now RAISEs on FIP-invalid shapes + 4-quadrant classifies status (admin/non-admin × complete/incomplete), `matches_select` RLS adds incomplete-as-member branch (4-way union), `matches_sets_valid_or_incomplete` CHECK constraint enforces shape rules with `status='incomplete'` bypass for historical data. Five named migrations (s045_match_validation_helpers, s045_rectify_existing_matches, s045_match_status_incomplete_trigger_rls, s045_match_sets_check_constraint). **Existing data audit + rectification:** 4 of 7 existing matches violated FIP — Match #1 (04/May) truncated 3-set→2-set (dead rubber), #2 (27/Apr) reclassified incomplete (both [6,5][6,5] sets invalid), #3 (20/Apr) truncated (invalid [6,5] 3rd set), #4 (17/Apr) truncated (invalid [6,5] 3rd set), #5 (06/Apr) reclassified incomplete (invalid [4,3] 2nd set), #6 (03/Apr) truncated (dead rubber 3rd), #7 (25/Mar) unchanged (genuine 3-setter). Strict FIP truncation rule (apply same dead-rubber drop on the 3 previously-valid 2-0 matches the user spotted) confirmed mid-session. SW v51→v52. Commit: e25ac9b. Deploy: dpl_HKHhKuNMn5cjVp7rJZ4EppbRPG9i READY. iPhone smoke test all passed. |

| S046 | 2026-05-06 | FT-12 Premier Padel Polish + Player Management | Feature/Build | Post-P7 | Issue #12 closed in 2 commits. v1 (87d4dbb): blended header gradient under status bar, italic uppercase PADELHUB wordmark, floating pill bottom nav, 2-col Players grid, italic admin roster rows, country flag slot in drill-in profile, `flagEmoji(iso3)` helper covering 42 codes, SW v52→v53. v2 (3bd48b0) addressing iPhone smoke test: header padding regression fix (S044 v3 tight 4px+0px restored), solid `BG`-colored pedestal behind floating nav (`pointerEvents:none` so taps pass through, hides scrolled content from gutter bleed-through), AdminDashboard rewritten — inline player list replaced with single "Player Management" button, new `PlayerManagement.jsx` screen + `EditPlayerModal.jsx` bottom sheet (photo upload via Supabase storage at `avatars/players/{id}.jpg`, country dropdown 42 ISO-3 codes A-Z, left/right position toggle, name/nickname). DB: `players.country` + `playing_position` + `avatar_url` columns, CHECK constraint on position, storage policies for `avatars/players/*` path, 13-row country backfill per user preset list (Padel Stars: PSE/IRQ/LBN/KWT/GBR + Ryan→DEU). Nav-bar lock explicitly waived in CLAUDE.md/lessons.md for #11+#12 — to be re-locked after #11. Mockup `padelhub/public/mockup-issue12.html` (4 phone frames + header A/B + live accent/font toggles) served via dev server, removed before commit per S043. Plans tracked in-project at `padelhub/planning/FT-12-premier-padel-polish.md` + `issue11-country-presets.md`. SW v53→v54. Both deploys READY (~7s + ~16s). iPhone smoke test passed both rounds. |

| S047 | 2026-05-06 | FT-13 Ranking Format + Terminology Renames (Issue #11) | Feature/Build | Post-P7 | Issue #11 closed in single commit c415b1a (5 files +79/-44). theme.js: nav labels Leaderboard→Ranking + Combos→Partners (tab IDs unchanged). App.jsx ranking screen redesigned: italic uppercase 'Ranking' h2 + active-season selector dropdown + caption row, new 8-column table (# · Player avatar+italic name · Country flag+ISO3 · MP · MW · ML · CW · Eff%) with gold/silver/bronze rank colors for top 3 and CW>=3 gold-highlight, podium retained, sort logic untouched. New 'Last 5 Matches · Form' strip below table (relocated from Players list per Issue #12 handoff). PlayerStats drill-in flash-cards renamed: Win Rate→Effectiveness, Games Played→Match Played, Wins→Match Won, Losses→Match Lost, Streak→Cons. Wins (5 swaps + bonus 'MOTM Leaderboard' analytics section→'MOTM Ranking'). CombosView: 'My Combos' sub-tab→'My Partners'. **Nav bar lock RE-APPLIED** in `padelhub/CLAUDE.md` Design System note + Workflow Rules #2 + `tasks/lessons.md` Critical Rule #2 (explicit current state recorded for future cold-start clarity). No DB migration needed — S046 already shipped country column + 13-row backfill; #11 just renders it. SW v54→v55. Deploy dpl_CUErfpcoTUiM4HdQFL6afjp1nWP1 READY (~10s build). flagEmoji import added to App.jsx. |

| S048 | 2026-05-06 | Issue #13 Admin Dashboard Consolidation | Build | Post-P7 | Issue #13 closed in single commit 1e6d58a (5 files +53/-48). Admin entry points consolidated into AdminDashboard. **Admin Management** section (owner-only member-role list) ported from SettingsView → AdminDashboard, between Roster and League Settings. **Platform Admin** button (super-admin only, `user.id === PLATFORM_ADMIN_ID`) ported from Sidebar → AdminDashboard bottom (after Data Export). Sidebar: 📩 Invite Players gated on `isAdmin` (was visible to all members); 🛡️ Platform Admin button removed; PLATFORM_ADMIN_ID import dropped. SettingsView signature trimmed (-5 props: `leagueMembers`, `memberProfiles`, `updateMemberRole`, `isOwner`, `league`). App.jsx: `updateMemberRole` added to LeagueContext, SettingsView prop list trimmed. SW v55→v56. Esbuild syntax check OK on all 4 JSX files. Deploy dpl_4hnSyoLjvV4s7xK4jnpsP8S4McHv READY. **Implicit assumption flagged:** Platform Admin button now only reachable when super-admin is also a league admin (true for current super-admin; documented for future). |

| S049 | 2026-05-06 | Issue #15 Nav + Header Polish + FT-14 Plan | Build/Plan | Post-P7 | Issue #15 closed in single commit d3200d6 (2 files +8/-7). Bottom nav: floating-pill `bottom` 14→6px above safe-area-inset-bottom; pill internal padding 8/6/10→6/6/8; pedestal 82→68px; wrapper paddingBottom 96→82px. Header: root cause was iOS rubber-band overscroll exposing wrapper `BG` color above the sticky header (perceived as "header too low at top, fine when scrolled"). Fixed via one-line CSS injection: `html,body{background:#0d0d14;overscroll-behavior-y:none;}` matches header gradient start so overscroll is invisible. SW v56→v57. Deploy dpl_8M45iowtz5nExKrYarYSSWoXeodx READY. **Plus:** wrote `padelhub/planning/FT-14-season-management.md` (~270-line plan for Issue #14): new `season_players` join table + idempotent backfill, 5 owner-only SECURITY DEFINER RPCs (create/end/update/reactivate/set_roster), `SeasonManagement.jsx` component mounted via `sidebarView === "seasonManagement"`, 3 stats-consumer options enumerated with recommendation (Option C hybrid). Plan file lives IN-project per Lesson from S043 (no more lost plans). Awaiting user approval before S050 implements. |

| S050 | 2026-05-06 | Admin Sweep + Season Mgmt + Profile Self-Edit + Country Picker | Build | Post-P7 | Big multi-commit session — 4 commits, ~1000 net lines added across 3 new components and 4 DB migrations. **#14 + #16 + #17 (a24f2b9):** new `season_players` table + 15-row backfill + 6 owner-gated SECURITY DEFINER RPCs via new `_assert_league_owner` helper (create/end/reactivate/update_season + set_season_roster atomic full-replace + update_league_name). Single-active-per-league enforced in create + reactivate via auto-deactivation. New `SeasonManagement.jsx` (~280 lines) accessible from Admin Dashboard → League Management. AdminDashboard restructured: removed S048's redundant Admin Management section, new League Management section (editable name + invite + season mgmt button). Sections: Roster → League Management → Data Export → Platform Admin. Scroll-to-top now fires on `sidebarView` change. Platform Admin Close → ← Back. PlayerManagement "hand" → "side". Player Management button subtitle stripped. **My Profile self-edit (985457c):** new RLS policy `players_update_self` (claimed user updates own row, blocks user_id/league_id changes). New `EditMyProfile.jsx` slim bottom-sheet for name/nickname/country/position. ProfileView gets country/position/nickname chips + ✎ Edit Profile button. **CountrySelect (594d89e):** new searchable combobox with auto-focused search input, scrollable filtered list, "— Not set —" clear. COUNTRIES expanded 42 → 195 (all UN states + Palestine + Taiwan + Vatican, sorted alphabetically by NAME, **Israel intentionally excluded**). `helpers.js` ISO3_TO_ISO2 map expanded to match. Replaces native `<select>` in EditPlayerModal + EditMyProfile. **Flag rendering + filter UX (f617c1c):** root cause of "PS letter blocks instead of 🇵🇸" was the inherited Outfit font lacking color-emoji glyphs on Windows. Fix: new `.flag` global CSS class with emoji-priority font stack (Apple Color Emoji / Segoe UI Emoji / Noto Color Emoji / Twemoji Mozilla / EmojiOne Color / Android Emoji). Applied to all 7 flag spans across 6 components. CountrySelect filter switched from substring to per-word startsWith ("p" → Pakistan/Palau/Palestine/...; "south" → all 3 South-x; "korea" → both Koreas). SW v57→v58→v59→v60→v61. All 4 deploys READY: dpl_E28bV... / dpl_9d8HU... / dpl_B15hg... / dpl_HnMGH.... |

| S051 | 2026-05-06 | Issues #18 + #19 Header Gap + Pairs Renames | Build/Fix | Post-P7 | 2 issues closed in 2 commits. **#19 (a310946):** Partners screen vocabulary aligned with Premier Padel — CombosView sub-tab Best Duos→Best Pairs + Top/Worst Partnerships→Top/Worst Pairs (3 strings); PlayerStats partnership analytics cards Best/Worst Partnerships→Best/Worst Pairs (2 strings). User-chosen scope: also include PlayerStats for consistency. SW v61→v62. Deploy dpl_t9JaNSP... READY. **#18 (ea7da90):** dynamic island gap at scrollY=0 fixed. Symptom: ~56px gap between dynamic island and sticky header at scroll top, perfect when scrolled. Root cause: S049 CSS injection patched bg + overscroll-behavior on html/body but never reset margin/padding; iOS PWA black-translucent mode left an implicit body offset that sticky's `top:0` masked once scrolled but exposed at scrollY=0. Fix: extended injection at App.jsx:779 to `html,body{margin:0;padding:0;...}` + `#root{margin:0;padding:0}`. Plus index.html theme-color #0a0a0f→#0d0d14 to match body bg + header gradient start (eliminates PWA launch-flash). SW v62→v63. Deploy dpl_4wj3V5T... READY. iPhone-confirmed working. **All open GitHub issues clear.** Used AskUserQuestion to scope #19 + capture #18 symptom screenshot before guessing — saved a blind comprehensive-fix iteration.

| S052 | 2026-05-06 | Issues #20 + #21 Photo Upload + Sidebar Profile (+ MOTM avatar) | Build/Fix | Post-P7 | 2 issues closed in 2 commits + 1 follow-up. **#20 (e439b0a, 6 files +67/-34):** TWO bugs. (a) "Failed to load on first attempt" iOS Safari PWA — replaced FileReader→dataURL→Image() decode chain with new `decodeImageFile(file)` helper at `utils/helpers.js` that prefers `createImageBitmap` (native HEIC decode on iOS 17+) with URL.createObjectURL+Image fallback; applied in both App.jsx uploadAvatar AND EditPlayerModal uploadPhoto. (b) Photo not propagating: ProfileView upload wrote only to `profiles.avatar_url` but player-avatar slots read `players.avatar_url`. Fix: when claimed user uploads/removes profile photo, App.jsx now write-throughs to claimedPlayer.avatar_url + triggers loadLeagueData(). PlayerStats.jsx 7 avatar slots updated via new `getAvatar(pid)` helper: drill-in profile top card, Insights Most Active, Best Pairs, Worst Pairs, H2H opponents (×2), Players grid. **#21 (e439b0a):** removed standalone "👤 My Profile" sidebar button + following divider; wrapped top user info block (avatar+name+email) in a button → opens My Profile. Added `›` chevron hint. **Follow-up (3b9ef98, 2 files +7/-3):** iPhone smoke caught MOTM Ranking inside Insights still rendering name-only — added 28×28 gold-tinted avatar circle with letter fallback + flex protections (minWidth:0 + ellipsis on name, flexShrink:0 on count badge). SW v63→v64→v65. Both deploys READY: dpl_Bxo181K... + dpl_GjtXb3U.... iPhone-confirmed all features. **All open issues clear.** Two AskUserQuestion calls scoped (a) PlayerStats coverage and (b) write-through behavior before touching code. New Lesson #47: anchor multi-site avatar sweeps on `getName(` (name-rendering) not existing avatar markup — MOTM was missed because it had no avatar circle to match.

| S053 | 2026-05-06 | Issues #22 + #23 Settings Reorg + Pair Card Avatars | Build/Fix | Post-P7 | 2 issues closed in 3 commits + 1 DB migration. **#22 (4117846, 4 files +79/-41):** Settings reorganization + self-serve account deletion. Account section moved from top to bottom (order is now Notifications → League → Account → Danger Zone). New Danger Zone with WORKING delete via new SECURITY DEFINER RPC `delete_my_account()` (migration `s053_delete_my_account_rpc`): refuses if user owns leagues (would orphan members), unclaims their players (preserves match history via NULL on user_id), deletes notifications/push_subscriptions/league_members/profile, then deletes auth.users row. Frontend signs out on success. Sidebar: removed "Switch League" button (already in Settings → League). **#23 (4117846):** Both Best/Worst Pairs cards in PlayerStats analytics→Partners restructured to 3-col flex: avatar A on left, names + W-L centered (with minWidth:0 + ellipsis), avatar B on right (flexShrink:0). Worst Pairs always renders with "No losing partnerships yet" empty-state. **Follow-up 1 (d22c57a, 2 files +11/-16):** Danger Zone progressive disclosure — default state shows ONLY red-outlined "Delete Account" button; warning copy + confirm flow revealed on tap. Bottom version row + divider removed. **Follow-up 2 (badc389, 2 files +7/-2):** Worst Pairs gate fix — `partnerships.length >= 6` threshold dropped from PlayerStats per-player view (it was league-scale logic copied to per-player scope, hiding worst-pair data for players with <6 partners even when CombosView at league scale showed plenty). SW v65→v66→v67→v68. All 3 deploys READY: dpl_Fi1eg31... + dpl_6s2y9Ya... + dpl_Bzut47x.... iPhone-confirmed end-to-end. **All open issues clear.** Three AskUserQuestion calls scoped delete-account behavior, worst-pairs empty-state, pair avatar layout. Two new lessons: #51 (recompute thresholds when porting between league-scale and per-player-scale views), #52 (progressive disclosure for destructive actions over always-visible safety wall).

| S054 | 2026-05-06 | GitHub Issues Batch #24 #26 #27 #29-34 | Fix | Post-P7 | 9 issues closed in 2 commits + 1 DB migration. Quick wins (243ed08): #27 GP→MP, #30 analytics pills equal-width, #31 Leaderboard medals 🥇🥈🥉, #32 header de-italicise+logo 36px, #33 filter dropdown removed, #34 admin regen icon inline. Medium batch (6ce404b): #24 preload lazy chunks + invisible LazyFallback, #26 nav 7→5 tabs (Partners+Rules removed, Rules→sidebar Official Rules button + overlay), #29 Platform Admin Promise.allSettled+Retry+hard delete user; DB migration `platform_delete_user` SECURITY DEFINER RPC. SW v68→v70. Cross-PC sync: local was behind repo; compacted session summary described edits that weren't on disk — required grep-verify + re-apply. Remaining open: #28 (complex admin overhaul) + #25 (new pairs leaderboard feature). |

| S055 | 2026-05-06 | Issue 28 Season Management Overhaul | Build | Post-P7 | Season data isolation (selectedSeasonMatches/seasonElo/seasonLb), awards redesign (Champion/RunnerUp/TopPair/MostActive/MostMOTM/BestStreak), SeasonManagement full-screen detail + location + delete, AdminDashboard simplified + Players rename, new LeagueManagement.jsx. Commit 8788712. SW v71. |

| S056 | 2026-05-06 | Issues 37 38 39 Season Awards Fixes | Fix | Post-P7 | #37 date overflow (minWidth:0 on flex children), #38 Match Won/Lost labels, #39 Top Pair UUID separator bug + layout + Cons.Wins color/label + italic removal. Commits 869087c, f1628f6. SW v73. |

| S057 | 2026-05-06 | Issues #36 + #40 Custom Nav Icons + Match Season Selector | Build | Post-P7 | #36: new NavIcons.jsx (Trophy emoji-style silhouette, Racket teardrop+perforations, Players trio, CrossedRackets), full-tab oval-capsule active state (rectangle→22px-radius), 20%-alpha tint + bold label when active. #40: LeagueContext now exposes selectedSeason+setSelectedSeason; MatchHistory dropdown filters approved+pending+incomplete by season_id, synced with Ranking selector. Issue #35 closed without code change (existing accent kept after side-by-side mockup vs WhatsApp green). Commits f0b3969, 523225c. SW v74→v75. |

| S058 | 2026-05-07 | GitHub Issue Sweep #42 #43 #24 #41 #26 + #25 Plan | Fix/Plan | Post-P7 | 4 fixes shipped + 1 issue closed without code + 1 plan drafted. **#42 (329f628):** bottom-nav active pill centering — root cause was parent `alignItems:"end"` + asymmetric padding 6/6/8 pinning the 44px pill to bottom of 56px row (12px top, 8px bottom gap). Fix: `alignSelf:"center"` on each icon button + container padding equalized to 6/6/6. Verified 7px/7px symmetric. **#43 (956c4a3):** iOS native rubber-band overscroll re-enabled — removed `overscroll-behavior-y:none` from html/body style; the load-bearing fix from Lesson #40 (body bg painted to #0d0d14 = header gradient start) still hides the rubber-band reveal. Documented Lesson #15 escalation if #18 regresses. **#24 (899ecdf):** loading flash on tab switch — `firstLoadRef = useRef(true)` gates `setLoading(true)` so only initial mount shows full-screen skeleton; subsequent loadLeagueData() calls (realtime, refresh, post-write) update silently. **#41 partial (9d38603):** auto-skip league picker for 1-league users — LeagueGate `autoSelectedRef` triggers `setSelectedLeagueId` when `userLeagues.length===1`. Larger onboarding rework held pending user templates. **#26 closed-without-code:** verified S054 already removed Partners+Rules tabs and added Official Rules to sidebar. **#25 plan:** drafted v1 at `padelhub/planning/FT-15-pairs-leaderboard.md` (DB additions, RPCs, 4 frontend changes, 6 open questions). SW v75→v76→v77→v78→v79. All 4 deploys auto-rolling on Vercel. |

| S059 | 2026-05-07 | Issue #46 Triage + Phase 1 Plan | Plan | Post-P7 | No code, no deploys. **GitHub housekeeping:** #44 (spring bounce) closed as duplicate of #46 Phase 2; #45 (Admin Dashboard polish) closed as duplicate of #46 Phase 8; #41 reframed via comment to fold the larger onboarding rework into #46 Phase 11 (stays OPEN as canonical tracking entry). Open issues now down from 5 to 2 (#46 master + #25 pairs deferred). **Issue #46 reference received:** user dropped approved JSX mockup at `padelhub/docs/PadelHub_Complete_v2.jsx` (2,425 lines, 22 screens). File indexed: Icon switch component (~50 icons) at lines 6–71, 3-part CSS token block at 76–887, mock fixtures + 17 screens covering all 11 phases. **Phased rollout strategy locked:** 11 phases × ~12–14 sessions including verification. One feature branch per phase, ship to live progressively (no feature flags), iPhone + Android Chrome smoke-test gates next-phase start. Phase 2 frozen for nav icon artwork (S057 NavIcons.jsx stays). Phase 11 is only DB-touching phase. **Phase 1 plan drafted** at `padelhub/planning/issue-46-phase1-foundation.md` (~250 lines): in/out scope, file-by-file changes (`src/index.css` token append, new `src/components/Icon.jsx`, new `padelhub/public/tokens-demo.html`), 15-item DoD checklist split between live-app and demo-page smoke-tests, rollback plan, 5 open questions, hand-off to Phase 2. **Decisions:** tokens use spec's long names (`--accent` not `--ac`); Phase 1 verified via standalone HTML demo first; NavIcons.jsx + new Icon.jsx coexist; do NOT touch index.css resets (load-bearing for Lessons #40/#44); no body bg change in Phase 1 (#080808 ≠ current #0d0d14, defer to Phase 2). Phase 1 is a deliberate visual no-op on live app — any visible diff post-deploy = regression signal. **3 validated patterns captured** in session log (plan-as-canonical-document, issue-consolidation-before-implementation, standalone HTML demo over in-app demo route). |

| S060 | 2026-05-07 | Issue #46 Phases 1+2+3 + 3 visual-regression fix PRs (live SW v85) | Build/Ship | Post-P7 | Phase 1 built on `feat/46-phase1-foundation` — [PR #47](https://github.com/mmuwahid/Padel-Battle/pull/47), 2 commits, both Vercel preview deploys READY. **Direct push to `main` was harness-blocked** — recovered with `git branch` + `git reset --hard origin/main` and pushed feature branch + opened PR. **Commit 30efd66:** `src/index.css` appended Phase 1 token block + Google Fonts (Syne + DM Mono) using spec's long names; `src/components/Icon.jsx` NEW with 56-case switch ported verbatim from `docs/PadelHub_Complete_v2.jsx` 6–71; `public/tokens-demo.html` NEW standalone verification page; SW v79→v80. Existing index.css resets untouched (Lessons #40/#44). No `body { background: var(--bg) }` rule (defer to Phase 2+). Local Vite + esbuild syntax checks both clean; live body bg stays #0d0d14, no console errors → visual no-op confirmed. **Commit 95c4a1f (user-feedback round):** user screenshot of `/tokens-demo.html` flagged that the spec's `racket / players / gamemode` SVGs didn't match the live bottom-nav silhouettes (S057 NavIcons.jsx). Replaced those 3 cases in `Icon.jsx` + `tokens-demo.html` with verbatim paths from NavIcons.jsx. Trophy left as spec version (not flagged). **NavIcons.jsx itself untouched** — bottom nav artwork stays frozen, captured as auto-memory `feedback_nav_icons_frozen.md` to enforce across all #46 phases. **Phase 2 plan drafted** at `padelhub/planning/issue-46-phase2-header-nav.md` (~180 lines): AppHeader + bottom-nav refactor with bottom-nav `.nicon` rendering `<NavIcon>` from NavIcons.jsx (NOT migrating to the new Icon.jsx), full CSS using token references, file-by-file changes, risk register against Lessons #40/#44 + S058 #42/#43, 12-item DoD checklist with 4 regression checks, rollback, 3 open questions, hand-off to Phase 3. **Phase 1 merged to main as `9325a55` via `gh pr merge 47 --squash`** (user said "post it to main", harness allowed via gh API even though direct git push was blocked) — production deploy `dpl_5jMBzDiv1VV7RmBDXSpVrUDBMvh3` READY, SW v80 live. **Phase 2 BUILT + SHIPPED in same session** on `feat/46-phase2-header-nav`: appended Phase 2 component CSS (`.hdr/.hl/.hr/.logo/.lm/.lt/.ibtn/.av/.bnav/.ntab/.npill/.nicon/.nlbl/.fab/.fab-wrap`) to `src/index.css` using token references only (no hardcoded hexes); replaced inline-styled AppHeader (lines ~843–876) with `<header className="hdr">` + class-based logo/refresh/bell/avatar; replaced inline-styled bottom nav (lines ~1323–1342) with `<nav className="bnav">` containing `.ntab` (with `.npill` scale-in spring + `.nicon` wrapping `<NavIcon>` from frozen NavIcons.jsx + `.nlbl`) + new `.fab` using `<Icon name="plus">` from new Icon.jsx; deleted Phase-1 verification artifact `public/tokens-demo.html`; SW v80→v81; pedestal removed (full-width nav has no gutters). PR [#48](https://github.com/mmuwahid/Padel-Battle/pull/48) squash-merged as `4f7f693` after user "merge and go" authorization, prod deploy `dpl_CBsBZb6uTH1DJjpQ2g5KA8jUFnZu` READY. **Edit-tool CRLF gotcha:** App.jsx had CRLF line endings; Edit's pattern-match silently failed on multi-line replace. Recovered with `tr -d '\r'` to LF then used Node script (`fs.readFileSync` + index slice + writeFileSync) for the bigger header + bottom-nav refactors — git auto-restored CRLF on commit via core.autocrlf=true. **Local Vite verification before push:** 1 `.hdr` (sticky), 1 `.bnav` (fixed), 4 `.ntab`, 4 `.npill`, 4 `.nicon`, 1 `.fab`, 2 `.ibtn`, 1 `.av` rendered on authenticated screen; tab switch toggled `.ntab.on` with scale-in pill spring; NavIcons artwork (Trophy/Racket/Players/CrossedRackets) preserved; no console errors. **3 lessons:** assume `main` is protected (#65); cross-check spec components against frozen live equivalents (#66); standalone HTML demo for foundation phases (#67); **NEW lesson #68:** for large file refactors that hit Edit-tool CRLF mismatches, normalize line endings or fall back to Node-script `fs` patches. **Phase 3 also shipped same session, then 3 visual-regression fix PRs** — PR [#49](https://github.com/mmuwahid/Padel-Battle/pull/49) squash-merged as `ac7cf35`, prod deploy `dpl_HwwynSgnF5sm2tneF94AxgeaBWwG` READY. AuthGate.jsx (login/signup/recovery) + LeagueGate.jsx (picker) rewritten with class-based markup using Phase 1 tokens. New CSS rules in `src/index.css`: `.lscreen/.lbg/.lhero/.llogobox/.lbrand/.ltag/.lform/.flbl/.fwrap/.finput/.pwtog/.lcta/.gbg2/.or-div/.llink/.lerr/.lok/.lgcard/.lgsection/.lginline` plus `@keyframes pg` glow-pulse on the logo box. Animated radial-gradient + 28px-grid backdrop on `.lbg`. Password field eye-toggle uses `<Icon name="eye"|"eye-off"/>` from Phase 1's Icon.jsx. Brand consistency held — kept PadelLogoSmall in `.llogobox` instead of swapping to spec's `<Icon name="trophy">`. Plan file at `padelhub/planning/issue-46-phase3-login-onboarding.md` (~250 lines). **Full 3-step OnboardingScreen with claim-existing-player + DOB/gender/playing_side fields explicitly deferred to Phase 11** (the only DB-touching phase) — Phase 3 ships the visual layer only. SW v81→v82. **Then 3 visual-regression fix PRs** after user feedback exposed that Phase 2/3 had silently overridden 12+ live tunings. **PR [#50](https://github.com/mmuwahid/Padel-Battle/pull/50) `a909b5e` (SW v83):** restored 4-color Google SVG (Phase 3 used spec's gradient-G letter) + initial fix for "rubber-band overscroll bugged again" by aligning body bg to `#080808`. **PR [#51](https://github.com/mmuwahid/Padel-Battle/pull/51) `d2cd482` (SW v84):** user said "header n bottom nav are ruined" — full forensic compare of S058 main `61d188a` vs Phase 2 found header padding `4px 16px` → `13px 18px` (S044 v3 lost), `padding-top: env(safe-area-inset-top)` dropped (Lesson #18 dynamic-island offset, the actual root cause of "rubber-band header bugged"), gradient bg → flat `rgba(8,8,8,0.92)`, `.lm` tinted logo box added (never asked for), nav `#12121af0` → `rgba(8,8,8,0.95)`, active pill 20% direct → `.npill` 9%+border+spring (half as visible), **pedestal removed** (Lesson #34 violated), FAB text `+` → `<Icon>` SVG with double shadow, body bg `#0d0d14` → `#080808` (Lesson #40 mismatch). All 12+ values reverted to S058 inside Phase 2's class-based CSS rules — markup architecture survives. Lesson #69 added. **PR [#52](https://github.com/mmuwahid/Padel-Battle/pull/52) `ec31fee` (SW v85):** user said "scrolled screen is passing header n footer" — S058's `rgba(18,18,26,0.94)` + `backdrop-filter: blur(20px)` was leaking blurred ghosts of scrolled content through. Bumped `.bnav` to fully opaque `#12121a`, dropped backdrop-filter, matched pedestal color so bottom block reads as one unbroken solid surface. **Live `padel-battle.vercel.app` now on SW v85 — all 3 phases shipped + all 3 regressions resolved.** **2 new auto-memory entries enforce future #46 discipline:** `feedback_nav_icons_frozen.md` (NavIcons.jsx artwork frozen across all phases), `feedback_issue46_dont_take_spec_literally.md` (mandatory pre-merge diff gate: list prior tunings → diff every spec value → classify spec-wins/prior-wins/ambiguous → ASK on ambiguous → run `getComputedStyle` checks pre-PR-open → never bundle architecture migration with visual changes in one PR). **5 new lessons:** #65 (assume main is protected, default to feat/PR + `gh pr merge`), #66 (cross-check spec vs frozen production equivalents before porting verbatim), #67 (standalone HTML demo for foundation phases), #68 (Edit-tool CRLF silent failure → normalize to LF or use Node-script `fs` patches), #69 (spec verbatim is not always optimal — diff against live tunings BEFORE merging, not after "ruined" feedback). Open issues after S060: #46 Phases 4–11 still pending; #25 pairs deferred. |

| S061 | 2026-05-07 | Issue #46 Phase 4 Ranking Screen Restyle + post-merge fix | Build/Ship | Post-P7 | Phase 4 class-based restyle of leaderboard/ranking tab. New CSS: podium pods (.pod.p1/.p2/.p3), 8-col grid table (.lbtable/.lbrow), form-dots inline, .lbrow.me highlight. PR #53 squash-merged as `7c3f740`, SW v85→v86. **Post-merge audit caught 8 broken inline-style aliases** (`var(--los)/--mu/--go/--mo` — silently rendering inherited color instead of red/grey/gold/mono). Fix shipped as PR #55 → `84cd00f`, SW v86→v87. |

| S062 | 2026-05-07 | Issue #46 Phase 5 Players Screen Restyle | Build/Ship | Post-P7 | Phase 5 class-based restyle of PlayerStats roster sub-view. New CSS: `.seg/.sb` segmented control, `.rbar` header, `.srchw/.srch` search, `.plist` 2-col grid of `.prow` cards, circle avatars, W-L inline, "YOU" badge. User picked Q1=C (hybrid 2-col), Q2=A (W-L shown), Q3=A (circle). Plan-first with AMBIGUOUS Q&A workflow. Lesson #70 grep audit pre-commit found zero short-aliases. PR #56 squash-merged as `1c92ece`. SW v87→v88. |

| S063 | 2026-05-07 | Search startsWith fix + League Section redesign PR1 | Build/Fix/Ship | Post-P7 | Two PRs shipped after S062 close. **PR #57 (`9c5be44`)**: strict displayed-value startsWith on PlayerStats roster search + CountrySelect + PlatformAdmin. Trade-off: typing "korea" no longer matches "South Korea". Search regression I introduced in S062 — Lesson #73 added (grep existing patterns before designing new search filters). **PR #58 (`6973f15`)**: League Section redesign architecture per user request "drop LeagueGate landing screen, build full league section in-app". User decisions: Q1=A (0-league empty-state on Ranking) / Q2=C (sidebar + header popover deferred to PR2) / Q3=A (localStorage `padelhub_lastLeagueId`) / Q4=A (`leagues.format` 'singles'/'pairs' locked at create) / Q5=defer (minimal v1 form). DB migration `s063_leagues_format_column` applied via MCP. LeagueGate slimmed 277→199 lines (state shell only). New LeaguesView.jsx ~280 lines with list/create/join/manage. Sidebar gets 🏟️ Leagues entry. Cold-launch goes straight into app — last-used league restored from localStorage; no more LeagueGate picker. SW v88→v89. |

| S064 | 2026-05-08 | Issue #46 Phase 6a Drill-In + Phase 6c Icon Sweep | Build/Ship | Post-P7 | Two PRs shipped on a clean cold-start day (no post-merge fixes). **Phase 6a — PR [#59](https://github.com/mmuwahid/Padel-Battle/pull/59) (`a3fda56`)**: drill-in player profile restyle. App.jsx conditional `.hdr` / `.hdr-back` render based on `tab==="stats" && selectedPlayer` (drill-in swaps logo for back chevron + PLAYER PROFILE title; refresh dropped from focused view). PlayerStats drill-in (lines 162–206) refactored from inline-styled markup to `.dpro` class block (~15 new classes): 96×96 circle avatar (Q2=C), gold OWNER / accent ADMIN role badge resolved via `player.user_id → leagueMembers`, country flag + playing-position tag pills, ELO + Effectiveness in hero (Q4=B), `.dpro-wr` win-rate progress bar, preserved 6-metric grid below (Match Played/Won/Lost + Cons.Wins/MOTM/Match Diff), achievements + H2H wrapped in `.dpro-sec-card` frames. CSS ~70 lines, LONG token names only. User picked Q1=B/Q2=C/Q3=A/Q4=B/Q5=B. Mockup-first via `padelhub/public/mockup-phase6a.html` viewed in Claude Preview. SW v89→v90. **Phase 6c — PR [#60](https://github.com/mmuwahid/Padel-Battle/pull/60) (`3370ab6`)**: 10 emoji/text icons swapped to `<Icon>` SVGs. App.jsx header: `↻`→refresh, `🔔`→bell. Sidebar.jsx: `✕`→close, `›`→chevron, `🏟️`→league, `📩`→user-plus, `📖`→book, `⚙️`→settings, `📲`→share (×2). Added `navBtnStyle` const for consistent flex+gap layout. NavIcons.jsx bottom-nav untouched (frozen). Pure JSX swaps, zero CSS. SW v90→v91. **Plan-as-deliverable:** drafted `padelhub/planning/issue-46-phase6-analytics.md` (~440 lines) at session start. Critical finding: spec's `PlayersScreen` Analytics tab body is unimplemented in spec; `ProfileScreen` (line 1694) is the only analog and it's the user's OWN profile, not a drill-in. Triggered Phase 6 split into 6a (spec-driven, low risk), 6b (discretionary, deferred), 6c (cleanup) instead of bundling. **Phase 6b held for S065**: needs Q6–Q10 answered (sub-tab style, card frame, League Activity chart, H2H selector, Insights composition). User said "I'll smoke test later" after authorizing both merges — iPhone verification pending. **No new lessons** (clean session). 3 validated patterns captured: spec-coverage-asymmetry → split PRs, mockup-first cadence holds across phases, `tab && selectedPlayer` as conditional-header trigger keeps shared AppHeader untouched. |

| S065 | 2026-05-08 | Issue #46 Phases 6b + 7 + Schedule Form Spec Port | Build/Ship/Fix | Post-P7 | **Four PRs merged in one session** (~5h). **PR [#61](https://github.com/mmuwahid/Padel-Battle/pull/61) `75cbf4f` (SW v91→v92):** Phase 6b — PlayerStats analytics views (lines 217–490) refactored to class-based markup. User decisions Q6=B (Phase 5 `.seg/.sb` 4-col via new `.seg-4/.sb-4`) / Q7=C (`.dpro-sec-card` frames) / Q8=B (spec `.elobars` chart) / Q9=A (native `<select>` H2H picker) / Q10=hybrid (Biggest Wins removed, Longest Winning + Losing Streak added — top-5 each, chronological per-player run-tracking inside `analyticsData` useMemo). **Bonus:** iOS-18 spring bounce on bottom nav restored — S060 PR #51 had hidden `.npill` with `display:none`. Phase 2 (PR #48) spring system reinstated on top of S060 saturation: `.npill` opacity 0→1 + transform scale(0.6)→scale(1) over 350ms cubic-bezier; `.ntab.on .nicon { scale(1.12) }`; `.ntab:active .nicon { scale(0.85) }`. **PR [#62](https://github.com/mmuwahid/Padel-Battle/pull/62) `a68f53d` (SW v92→v94):** Hotfix bundle — 6 iPhone-smoke regressions: `.an-body > .seg-4 { margin: 0 }` contextual override (pill was 36px from edge stacking margin+padding); "Roster (N)" → "Players (N)" rename; edit/trash emoji → `<Icon>`; leaderboard country cell ISO3 letters dropped (flag-only); rank `#` column `align-self: center`; Phase 6b sub-tab emojis 📈🤝⚔️💡 → SVG Icons (`trending-up / users / swords / bulb`). 3 new Icon cases added. **PR [#63](https://github.com/mmuwahid/Padel-Battle/pull/63) `bf0a9d4` (SW v94→v95):** Phase 7 — Match cards + ScheduleView simplification + Schedule form spec port. User decisions Q1=A (MOTM absolute-centered gold pill in `.mhd2`) / Q2=A (WIN/LOSS label ABOVE team col) / Q3=C (24×24 avatars, no flags) / Q4=A (vertical 80px score column with **"Final 2-1"** set count chip — user revision from spec's cumulative game total) / Q5=C (incomplete = 60% opacity + 50% saturate hybrid) / Q6=C (My-Pending: collapsible header + `.mcard.pending` inner) / Q7=B (approvals: card frame restyle, KEEP text labels) / **Q9 = DROP Past tab from ScheduleView entirely** (once played, matches surface in MatchHistory). MatchHistory.jsx + MatchApprovalsQueue.jsx full rewrites; ScheduleView.jsx restructured. New helper `setsWonCount(sets) → [a,b]`. Action buttons → `<Icon name="share|edit|trash"/>`. Phase 7 CSS ~200 lines. **3rd commit on same branch (`046109d`) — Schedule form spec port:** my first `.sform` draft didn't match user's reference screenshots; re-ported from `padelhub/docs/PadelHub_Complete_v2.jsx` ScheduleScreen lines 1490–1622 verbatim. Step 1: `.sch-progress` + `.tcard > .tcardh ("PLAYERS" + .shufbtn) > .tinner 3-col grid` with Team A (green dot/label) / `.tcolvs "VS"` / Team B (gold dot/label), `.pslot > .psel.af|.bf + .pselch` chevron-overlay selects. Step 2: `.svsum` summary chip + `.tcard > .shlbl` icon-prefixed labels (calendar/clock/court-l/edit), `.shi` inputs, `.stog/.stogbtn` inline duration toggle, `.inote` info banner. **Bug fixed in process:** ScheduleView never imported `Icon` — JSX threw on mount, ErrorBoundary masked to blank screen. One-line fix `import Icon from './Icon'`. 306-line plan-as-deliverable at `padelhub/planning/issue-46-phase7-match-cards.md`. **PR [#64](https://github.com/mmuwahid/Padel-Battle/pull/64) `6414b65`:** Premier Padel team-name format. User-flagged via iPhone — chip should read "Hamza / Basel vs MAK / Mano". Single-line change in `helpers.js`: `formatTeam(p1,p2)` separator `x` → `/`. Per CLAUDE.md "always use formatTeam" rule, applies globally to all ~50 call sites with zero JSX edits. Also fixed `.svsum` chip's manual `.map(getName).join(" & ")` → `formatTeam(getName(tA[0]), getName(tA[1]))`. **4 new lessons:** #74 (Icon import audit before adding `<Icon>` JSX — ErrorBoundary masks the crash), #75 (stacked margin+padding silently doubles spacing on small viewports), #76 (set-count vs game-total disambiguation — pin "total" semantic in plan), #77 (formatTeam single source of truth — no inline `.join(" & ")` allowed). **6 validated patterns:** hotfix bundling 6 fixes in one PR (one preview = one smoke pass), multi-step user Q&A across multiple AskUserQuestion calls, plan placeholder + Q&A → mockup → user approval → build → ship cadence (3rd consecutive #46 phase), DROP redundant feature when user signals it (Q9 Past tab), spec-port over my own design when user references spec, one-line global helper update beats N inline edits. |

| S066 | 2026-05-08 | Issue #46 Phases 7v2 + 8 + 9 + 11 + 12 (~95% master redesign done) | Build/Ship/Fix/DB | Post-P12 | **9 PRs + 3 DB migrations in one massive day (~10h)**. Phase 7v2 (#65 c56430f, SW v95→v96): match card v2 with split per-team score grid, FINAL col with "2-1 / 16-12", 8-emoji reaction popover, ranking globe header. Phase 8 (#66 801da69, SW v96→v97): `s066_add_players_gender_column` migration, gender filter pills with All/Men ♂/Women ♀ behind sliders icon. Phase 9 (#70 f76185a, SW v97→v98): full LogMatch/EditMatchModal/TeamShuffler restyle with `.modebar/.sccard/.cstep/.livebi/.acbtn/.mvpcard/.savebtn`. Phase 11 (#72 103307c, SW v98→v99): `s066_add_players_date_of_birth` migration + new OnboardingScreen 3-step wizard. Phase 12 PR1 (#73 e698d76, SW v99→v100): `s066_extend_playing_position_any` migration + EditMyProfile fix (court-l/r/any icons + "Any" label + 5-mandatory-fields validation) + Sidebar/Settings/Profile full restyle + gear icon swap + sidebar league subtitle + press-state chevron. PR2 (#74 a41ce8c, SW v100→v101): Ranking globe header + Rules first pass. Fix (#76 0fe169d): Rules emojis→Icon SVGs + switch icon for Switch League. Spec port (#77 a523e97): Rules collapsible cards verbatim per spec class names + search + filter pills All/Rules/Disputes with counts. **10 new lessons #78-#87. Issue #46 master redesign now ~95% done.** Open: PR 3 admin batch (5 mgmt screens) + Pending/Rejected approval-gated join workflow. 🐛 RulesView "undefined…" preview bug to fix first thing in S067. |

| S067 | 2026-05-08 | Issue #46 Phase 12 PR3 + Approval Workflow + 12 hotfixes | Build/Ship/Fix/DB | Post-P12 → ~99% master redesign | **14 commits, 1 PR (#78) merged, 3 DB migrations, ~12h day.** PR #78 (`b5fb06f` squash of `15b4806`+`cb3f586`, SW v101→v102): admin batch port — 5 components (AdminDashboard/LeagueManagement/SeasonManagement/PlayerManagement/PlatformAdmin) to spec class names, Rules SECTIONS data restructure with .rtag chips, Leaderboard `.lbh.c` rank header centering, sidebar X-icon dropped, EditPlayerModal gallery picker. **S067-r1 (`8807241`, SW v102→v103):** back buttons surfaced, Rules per-card press-state vertical accent, Most Argued zap icon, .rcontent mono+softer color, italic dropped on .adh1/.plmtit/.secname/.shtitle, EditPlayerModal spec gender+position, EditMyProfile DOB box-sizing fix, NotificationCenter full emoji→Icon rewrite, header refresh→pull-to-refresh, Season inline footer actions, PlatformAdmin rename-league. **S067-r1.1 (`1c7a4e8`, SW v103→v104):** **CRITICAL Rules-of-Hooks fix** — pull-to-refresh hook block was AFTER early returns (Lesson #25 BF-32 violated), moved up. Age tag (new getAge helper), Match Won/Lost rename, "Best Streak"→"Consecutive Wins", "MOTM Awards"→"Man of the Match" with gold star badge, .propic 96→130px, Remove Photo red action button, save-error diagnostic logging. **S068 Approval-gated join workflow (`15b9176` initial — BUILD ERROR; `36d73fb` PadelLogo→icons import fix; `ad2a240` LeagueGate try/finally guard; `b372c92` CRITICAL TDZ fix — loadJoinRequest declared after useEffects that referenced it in deps → React mount failed; `d582ec5` Achievements spec restyle; `9c2c6c7` invite-code RPC fix; `7c52134` chevron-left Icon alias; `88f3b5a` deletePlayer also removes league_members; `0578591` PlatformAdmin DELETE confirm; `d9feeb9` achievements title centered + count badge):** new `join_requests` table + RLS + 3 SECURITY DEFINER RPCs (create/approve/reject), 3 new screens (PendingApprovalScreen, RejectedScreen, ApprovalQueueScreen), OnboardingScreen step 3 rewired from immediate join → create_join_request, LeagueGate routing for pending/rejected, AdminDashboard new "Approval Queue" .adcard, App.jsx Matches tab .alban banner. Photo upload single-retry pattern (250ms backoff) bundled. SW v104→v107. Plan-as-deliverable at `padelhub/planning/issue-46-approval-gated-join.md` (240 lines). **7 new lessons #88-94:** TDZ from late useCallback declaration, sibling-import grep before adding, Icon case existence audit, RPC vs direct query for non-member RLS, league_members orphan from delete, deploy-state verification, single-retry pattern for iOS PWA storage. **6 validated patterns:** idempotent SECURITY DEFINER RPCs, polling for approval transitions, :active::before press-state, plan-as-deliverable BEFORE scope expansion, deploy verification after every push, 250ms single-retry for storage uploads. **Issue #46 master redesign now ~99% done.** |

| S068 | 2026-05-09 | Mega-Bundle Polish + Sidebar History + Syne Sweep | Build/Ship/Fix | Post-P12 → polish | **10 commits, 6 GitHub issues closed (#75, #68, #41, #46, #67, #82), SW v117 → v125, ~14h cross-day session.** `d94b200` legacy claimPlayer form → Pending Review lock screen. `ee15a42` Issue #67 League menu "N players · M matches" status + Issue #82 dropped YOU pill (replaced with green border tint, later refined to vertical bar in `dd33162`). `71efb86` 5-fix bundle: W/L color coding (green/red when >0, grey when 0) in roster pills + drill-in cells + reactions race fix (stable matchIdsKey + cancelled flag — no more disappear-after-1s) + duplicate admin notification fix (set_member_role RPC was sole source, push-notify also was inserting; dropped client-side push call) + green claimed-dot now only on UNCLAIMED players. `dd33162` unified back-button pattern (PlayerStats drill-in moved from .hdr-back to .back-btn-row, app header now consistent everywhere) + drill-in origin restoration (Ranking → drill-in → back returns to Ranking) + Player Management header simplified (LEAGUE eyebrow + redundant Players subhead dropped) + ALL italics stripped (12 CSS rules + 10 inline JSX). `08b7036` sidebar history stack (Settings → Admin → PlayerMgmt → back × 3 returns to drawer) + PadelHub logo home link (clears state and routes to Ranking). `9ecb4bd` drill-in Match Won/Lost cells now grey when 0. `728e68d` MISFIRE — `cp -r src/ /tmp/...src/` resolved into nested src/src/ tree; ghost-deployed without actual sweep. `1455da4` FIX — removed bogus tree + applied real Outfit→Syne sweep across 40 CSS + 26 inline JSX occurrences + wordmark .lt 14px/900 → 15px/800 per user direction (numerics on DM Mono untouched). `53b127e` + `5a0d249` Player leaderboard header centered. **3 new lessons #54-56:** cp trailing-slash semantics under MSYS Bash differ from GNU coreutils (verify via diff stat before commit), Edit "modified-since-read" means change WAS NOT applied (re-Read + re-Edit), useEffect dep churning array refs cause infinite re-runs that race with optimistic state (stabilize via string join). **6 validated patterns:** drillInOrigin single-string nav memory, sidebar history stack with null sentinel for drawer-origin, direct DB query via Supabase MCP to confirm duplicate-row sources, race fixes belong on dep array not consumer, sweep CSS variables when usage is fragmented (Node script + regex), single-class centering API (`.lbh.c`) for grid headers. |

| S069 | 2026-05-09 | Cropper + Lightbox + Nav Burst + Title Spec + Color Audit | Build/Ship/Research | Post-P12 → polish | **1 PR (#85, commit `25f7a2d`), 4 features + 1 spec align + 1 research deliverable. SW v125 → v126, ~2h, 466 + / 45 - lines.** **AvatarCropModal** (new, react-easy-crop wrapper): circle 1:1 crop + pan + 1×–4× zoom slider; outputs 200×200 JPEG blob to existing storage upload pipeline; wired into App.jsx uploadAvatar (My Profile flow) and EditPlayerModal uploadPhoto (admin player edit) — replaces the auto center-crop logic. **AvatarLightbox** (new): WhatsApp/Instagram tap-to-expand for ProfileView .propic and PlayerStats drill-in .dpro-pic; click/Esc dismiss, body-scroll lock, .92 backdrop. **Issue #81 — iOS-18 nav press burst:** `.ntab::before` radial-gradient halo (64px) scales to 1.45 + translates -26px above the bar (~36px overflow above nav); icon lifts translateY -10 and scales 1.28; .bnav + .ntab set overflow:visible; pure :active CSS holds while pressed. **Screen titles spec align:** dropped `text-transform:uppercase` from `.lbtitle / .adh1 / .lv-title / .sched-title` so Syne 800 mixed-case renders ("Leaderboard", "Dashboard", "Leagues", "Scheduled") matching JSX spec. Sub-section labels (.secname/.shtitle/mono eyebrows) intentionally kept uppercase. **Color audit (research-only deliverable):** :root tokens 100% match spec at variable level — but 395 hardcoded color refs scattered across index.css. Frequency table reveals top drift = `#9090a4` × 119 (NOT in spec; spec says `--muted #555555`). Three options surfaced (A1 keep `#9090a4` as documented divergence / A2 sweep to spec / A3 redefine `--muted` to `#9090a4` — recommended). User to decide A1/A2/A3 before sweep ships. **No new lessons** (all patterns are validations of existing techniques: `:active` CSS for tactile hold, spoofed-class verification when forceState unavailable, overflow:visible on parent+child for burst pseudos, class-name CSS strip > JSX rewrite, token audit before usage audit, frequency table > exhaustive scan). |

| S070 | 2026-05-09 | Quick Wins Sweep + Notif Click-Through + Loading Screen Redesign | Build/Ship/Fix/DB | Post-P12 → polish | **4 PRs merged + 1 issue closed via comment, SW v126 → v130, ~3-4h, 6 GitHub issues closed (#81 / #84 / #69 / #83 / #79 / #80), 1 DB migration.** **PR [#86](https://github.com/mmuwahid/Padel-Battle/pull/86) `4e8a9c8` (SW v126→v127):** quick-wins bundle. Closes #84 (DOB picker overflow — durable iOS fix via `.fgrp { min-width: 0 }` + `-webkit-appearance: none` + `::-webkit-date-and-time-value` padding reset; the recurring breakage point was the parent constraint, not the input itself), #69 (invite share preview hygiene — `og:image?v=126` cache-buster + `og:site_name` / `og:locale` / `og:image:secure_url` / `og:image:alt` / `twitter:image:alt` / `apple-touch-icon` 180×180 + precomposed fallback), #83 (handedness + Court Position rename — DB migration `s070_add_players_handedness` adds `players.handedness text NULL` with CHECK left/right/NULL; new Handedness toggle in EditMyProfile + EditPlayerModal rendered ABOVE Court Position; "Playing Side" → "Court Position" rename across EditMyProfile, EditPlayerModal, OnboardingScreen, ProfileView display, PlayerStats drill-in display; handedness display tag rendered BEFORE court position per spec; `handedness` added to `players` column select in App.jsx loadLeagueData). **PR [#87](https://github.com/mmuwahid/Padel-Battle/pull/87) `ea7d506` (SW v127→v128):** #79 NotificationCenter click-through. New `notificationTarget(n)` router function + `onNavigate` prop on NotificationCenter. App.jsx `handleNotifNavigate` callback (declared AFTER `sidebarHistory` state for TDZ safety per Lesson #88) closes sidebar/notif drawer then routes by target shape: `match + match_id` → Matches tab + scroll to `.mcard[data-match-id]` + 1.6s `.nc-flash` pulsing border; `members + join_approved + player_id` → Players tab + drill into player; `members + join_pending + request_id` → Approval Queue (admin); `ranking` → Ranking tab; `tournament/challenge` → Game Mode; `rejected` / `role_change` → no-op (just mark-read). MatchHistory got `scrollToMatchId` + `onScrolled` props; useEffect with 120ms delay finds `.mcard[data-match-id]`, smooth-scrolls into center, flashes highlight, calls onScrolled. data-match-id added to both `.mcard` renders. Verified live: 33 notifications → 16 navigable / 17 informational; clicking `members + join_approved` drilled into player profile. **PR [#88](https://github.com/mmuwahid/Padel-Battle/pull/88) `2c9cb1f` (SW v128→v129):** #80 loading screen redesign. PadelHubMark SVG redesigned with multi-layer depth (radial-gradient halo + outer dashed rotating ring + inner solid ring + larger center orb + outer pulse aura + 6 staggered satellite dots + central contrast point). 4 new @keyframes (`hubOrbit` 18s linear, `hubPulse` 1.6s scale, `hubAura` 1.6s counter-phase scale, `hubDot` 2.4s stagger). `.lhero` `justify-content: center → flex-start` + `padding: 32px → 30vh 32px 28px` so logo sits ~20% above true center. Static splash in index.html mirrored verbatim. Pull-to-refresh upgrade: `.ptr-overlay` small spinner replaced with `.ptr-splash` full-screen overlay rendering same `.lscreen` design. **PR [#89](https://github.com/mmuwahid/Padel-Battle/pull/89) `d3ada41` (SW v129→v130) follow-up fix:** user reviewed PR #88 ship and flagged "logo is in a black box container. it looks cheap. make sure the logo is basically on a transparent background so it blends into background of app". The radial-gradient halo + outer dashed orbit + inner solid ring were collectively rendering as a perceived "container box" against the dark app bg. Removed all three plus `@keyframes hubOrbit` + `.lhmark-orbit` class + central dark contrast point. Logo now consists of: 6 connecting lines, 6 staggered-fade satellite dots, center pulsing orb, outer pulse aura — transparent against `var(--bg)`. Drop-shadow softened 36px→24px / 0.35→0.30 alpha. Index.html static splash mirrored. **No new lessons** (all patterns validated existing techniques). **6 validated patterns:** backlog triage table → user picks cadence; recurring-regression hardening (find the missing assumption from first fix attempt); TDZ-safe useCallback (declare AFTER referenced state setters); static + React splash mirror always (Lesson #98); drop-shadow + halo gradient + bounded ring = "container box" perception (transparent-on-dark logos should be mark-only); same-day double-PR fix is fine when feedback is fresh. **Color sweep deferred** — Note A (`#9090a4` × 119 vs spec `--muted #555555`) still awaiting user A1/A2/A3 decision. |

| S071 | 2026-05-10 | Body Font Fix + Game Mode Redesign + Format Rules + Avatar Lightbox Circle | Build/Ship/Fix | Post-P12 → polish + Phase 10 ~75% | **9 commits push-direct to main, SW v130 → v137, ~6h, 0 GitHub issues closed (Phase 10 self-flagged), 0 DB migrations.** **`5719f2e` (SW v131)** body font cascade fix per user "the font for the headers of each screen still doesnt match" — `body { font-family: 'Syne', sans-serif }` so `.lbtitle` / `.adh1` / etc. that didn't explicitly declare font-family now inherit Syne instead of UA serif fallback. **`0c69b70` (SW v132) PR-A** Game Mode Phase 10 shell — new `gm-*` class system (gm-h header with eyebrow/title/sub, gm-seg 2-tab, gm-card mode picker with .on / .on.gold variants, gm-pblk player chips, gm-grid2 Courts/Points, gm-startbtn primary CTA). GameMode.jsx orchestrator rewritten (drop OLD A/BG/CD/BD constants, page header + segmented Casual/Competitive control + 3 mode cards SE/DE/RR with trophy/refresh/award icons). AmericanoMode selector rewritten (Americano/Mexicano cards with target/trending-up icons, player chip grid, courts/points selects, start button with disabled state). **`6e5fb21` (SW v133)** added Tournament Formats section to sidebar Rules with 5 collapsible cards (Americano/Mexicano/RR/SE/DE) — user reverted next message ("no its fine keep the rules to be inside game modes section"). **`0f6878e`** Revert. **`1bc60c5` (SW v133) PR-B** SE/DE/RR setup forms — new gm-setup-* class system (page header + tournament name input + gold format pill + gm-tcard team cards with editable name + 2 player selects + gm-trm remove btn + gm-addteam dashed Add Team + gm-startbtn Create). Edit tool em-dash quirk fixed: use `&mdash;` HTML entity not literal — (Lesson #100). **`5821774` (SW v134) PR-C** AmericanoMode active view — gm-actbar header (eyebrow + Syne 800 title + mono sub + End/Delete buttons), gm-stand live standings card with rank colors (gold/silver/bronze), gm-rndcard rounds with gm-mtch matches and ScoreSteppers. Complete view: page header + final standings card + Clear Results btn. Team separator ` x ` → ` / ` per Lesson #77. **`fccd988` (SW v135)** score-card team display fix per user "not enough to just show team a or b or c the team players needs to show so we know who wins" — SE Enter Scores cards + DE losers bracket + Grand Final + RR round cards now show team name (Team A) on top + player names (Husain / Moody) in mono below. Format Rules embedded in Game Mode bottom (FORMAT_RULES inline data + FormatRuleCard component reusing .rcard pattern from RulesView). **`57e1c70` (SW v136)** Format Rules split per tab per user "keep americano and mexicano rules in with that tab and the rules for rr elimitantion and double in the comptitive tab" — CASUAL_FORMAT_RULES (Americano + Mexicano) renders inside Casual tab, COMPETITIVE_FORMAT_RULES (SE + DE + RR) renders inside Competitive tab. **`42c87e7` (SW v137)** avatar lightbox circle fix per user — `.alb-img` width/height = `min(82vw, 82vh)` square + `border-radius: 50%` + `object-fit: cover` = perfect circle regardless of source aspect ratio. **2 new lessons (#99, #100):** body-level font-family completes a font sweep (class-by-class catches explicit declarations only — body falls back to UA stylesheet); JSX text Unicode escapes don't interpolate (use HTML entities like `&mdash;` / `&middot;` instead of literal — or `\u` escapes; Edit tool's CRLF-swap can corrupt literals). **7 validated patterns:** body-level font cascade, three-PR shipping cadence (entry surface → setup forms → active state), PowerShell Copy-Item for OneDrive ↔ /tmp sync (cleaner than perl byte-offset), HTML entities for JSX Unicode, recurring-regression fix posture (find missing structural assumption from first fix attempt), honest-answer + offer-to-do (when user says "we used to have X" but git says no), user-reverted feature can come back in a different location (Format Rules sidebar → reverted → Game Mode embedded → split per tab). **SE/DE/RR active tournament views still using OLD theme constants** — bracket renderer + score input + advancement logic deferred (brittle getElementById score input needs state-based refactor first). **BracketSVG.jsx color tokens** still pending. **Color sweep Note A** still awaiting A1/A2/A3 decision from S069. |

| S072 | 2026-05-10 | Gold-Orb Logo Redesign + DB Foundation for FT-15 + FT-16 | Build/Ship/DB/Plan | Post-P12 → polish + new features groundwork | **1 push-direct commit `ece8faf`, SW v137 → v138, 9 DB migrations applied via Supabase MCP, 2 plan-as-deliverable files written, 2 mockups reviewed and revised by user.** **Logo redesign:** new `PadelHubMark` SVG — central 3D gold orb (radial gradient `#fde68a → #f59e0b → #78350f` with top-left highlight ellipse for depth) + 6 satellite orbs orbiting around it via `hubOrbit` 22s linear group rotation + soft outer aura via `hubAura` 2.4s breathing + per-satellite scale stagger via `hubSat` 2s ease-in-out at 0/180/360/540/720/900ms delays. New `PadelHubMarkHeader` no-aura variant for in-app header `.logo .lm` slot at 32px (replaces `PadelLogoSmall`). `index.html` static splash mirrored verbatim per Lesson #98. Wordmark "Hub" tinted gold (`#f59e0b`). `index.css` filter switched to gold `rgba(245,158,11,0.40)`. SW bumped v137→v138. **DB foundation FT-16 (5 migrations):** `s072_open_matches_table` (open_matches lifecycle table), `s072_open_match_players_table` (join table with unordered uniqueness), `s072_matches_open_match_fk` (matches.open_match_id FK + AFTER INSERT trigger to flip open_matches.status='completed'), `s072_open_match_rls` (SELECT policies gated to league members), `s072_open_match_rpcs` (6 SECURITY DEFINER RPCs: create_open_match / join_open_match with auto-shuffle on lock / leave_open_match / cancel_open_match / set_open_match_teams manual override / expire_stale_open_matches sweep). **DB foundation FT-15 (4 migrations):** `s072_seasons_format_column` (additive column with CHECK 'individual'|'pairs', defaults to 'individual' so all 2 existing seasons unaffected), `s072_pairs_table` (pairs entity with elo seeded at 1500 Premier-Padel-style + LEAST/GREATEST unordered uniqueness index), `s072_pair_rpcs` (3 admin-gated RPCs + resolve_pair_id helper), `s072_pair_elo_trigger_v2` + `s072_pair_elo_trigger_function` (split into two after migration #9 errored on `update_season` parameter rename — recovered by DROP-then-CREATE of both existing overloads + recreate as 6-arg version with `p_format` added; pair-ELO trigger fires only on approved matches in pairs-format seasons where both pairs are registered). **Plan-as-deliverable:** `planning/FT-16-open-match-voting.md` (new) + `planning/FT-15-pairs-leaderboard.md` (v2 with all 6 user decisions baked in). **Mockups:** `public/mockup-ft15-pairs.html` (revised with inline-avatar pairs + Premier-Padel broadcast naming + ELO column dropped from leaderboard table per user direction) + `public/mockup-ft16-open-match.html` (revised with split TEAM A / TEAM B header in locked card per user direction). **Frontend wiring deferred to S073/S074** — each feature is a focused session per the plan estimates; advisor-validated "ship what's solid, stop, report back" path. **2 new lessons (#101, #102):** `CREATE OR REPLACE FUNCTION` cannot rename input parameters at the same ordinal position (must DROP first or add new param trailing) — encountered when extending `update_season` with `p_format`; multi-statement Supabase MCP migrations are all-or-nothing transactions — one failed statement rolls back even the safe parts of the same migration, must re-apply the safe parts in a separate clean migration. **No GitHub issues closed** (#71 and #25 still OPEN — close after frontend ships in S073/S074). |

**Next session:** S073 — **Production live at padel-battle.vercel.app on SW v138, main commit `ece8faf`** (Vercel BUILDING at S072 close — verify READY at S073 cold start). S072 closed with **1 push-direct commit + 9 DB migrations + 2 plans + 2 mockups, ~5h day**. Gold-orb logo live everywhere `<PadelHubMark>` renders + new `<PadelHubMarkHeader>` in app header `.logo .lm` slot. DB foundation for both FT-15 (Pairs Leaderboard) and FT-16 (Open-Match Voting) shipped — additive only, zero behavior change for existing flows. **Cold-start direction options for S073:** (1) **iPhone smoke test of S072 ship** — verify gold-orb logo on PWA after SW v138 cold-load: AuthGate splash, LeagueGate splash, in-app header `.lm` slot, index.html static splash, refresh transitions. (2) **FT-16 frontend** (recommended next per plan — smaller surface, more contained than FT-15) — per `planning/FT-16-open-match-voting.md` commit sequencing C2-C5: ScheduleView Open Matches section + claim/leave/cancel + form Private/Open toggle + LogMatch pre-fill + NotificationCenter renderer + push-notify Edge Function branch. App.jsx must also load `open_matches`/`open_match_players` in `loadLeagueData` and run `expire_stale_open_matches` sweep. (3) **FT-15 frontend** (larger — 4 components + 1 new) — per `planning/FT-15-pairs-leaderboard.md` v2 commit sequencing C2-C4: SeasonManagement Format toggle + Pairs Roster admin UI + LogMatch/ScheduleView pair-aware picker + new `PairsRanking.jsx` component (~250 lines, 7-col layout per Premier-Padel broadcast style with country flags below player names, no ELO column, podium mirrors normal league podium). (4) **Color sweep Note A from S069** — still awaiting A1/A2/A3 decision. (5) **Game Mode Phase 10 PR-D** (SE/DE/RR active tournament views, needs state-based score input refactor first). (6) **Game Mode Phase 10 PR-E** (BracketSVG color tokens). User picks at cold-start.

**Earlier S071 cold-start options (pre-this-session, archived):** S071 — **Production live at padel-battle.vercel.app on SW v137, main commit `42c87e7`.** S071 closed with **9 push-direct commits, SW v130 → v137, ~6h day**. Game Mode Phase 10 redesign ~75% done (selector + Casual full + setup forms full + Americano active+complete; SE/DE/RR active views + BracketSVG remaining). Format Rules embedded in Game Mode (split per Casual/Competitive tab). Avatar lightbox now opens in a perfect circle. Body-level Syne now cascades to all elements. **Cold-start direction options for S072:** (1) **iPhone smoke test of S071 ship** — 9 commits to verify on PWA after pull-to-refresh picks up SW v137. Especially body-font cascade (everything should look more polished at title level), Game Mode redesign (Casual + Competitive flow), score-card player names, format rules per-tab, avatar lightbox circle. (2) **PR-D: SE/DE/RR active tournament views** — bracket header bar + score-input cards + standings using gm-* classes. Has brittle `getElementById` score input — needs state-based refactor before classes work cleanly. Branch: `feat/46-phase10-pr-d-active-views`. (3) **PR-E: BracketSVG color tokens** — currently uses theme constants A/GD/etc., works fine visually but inconsistent with token system. (4) **Color sweep (Note A — S069 carry-over)** — answer A1/A2/A3 first then sweep. (5) **#71 — Open-match voting** (big feature, needs plan + DB schema). (6) **#25 — Pairs leaderboard** — DB-touching, 6 user questions still pending in `padelhub/planning/FT-15-pairs-leaderboard.md`. User picks at cold-start.

**Earlier S070 cold-start options (pre-S071, archived):** S070 — **Production live at padel-battle.vercel.app on SW v130, main commit `d3ada41`.** S068 closed with **10 commits, 6 GitHub issues closed, SW v117 → v125, ~14h day**. Issue #46 master redesign now ~99% done. Approval-gated join workflow shipped + E2E tested live + 8 hotfixes during session. Production splash now uses unified `<PadelHubMark>` (single logo from index.html static through React mount). Trash buttons gated behind edit-unlock pattern in PlatformAdmin + PlayerManagement. Cold-start direction options for S068: (1) E2E re-test of approval workflow with the latest hotfixes (last test surfaced GRANT bug + post-approval routing bug — both fixed in same session); (2) Photo cropper/zoom in avatar uploader (user explicitly deferred to "later"); (3) EditMyProfile design diffs — user flagged but never sent the comparison screenshot; (4) Claim-during-onboarding flow — invite-code path currently always submits as new_profile; spec also wants users to claim an unclaimed existing player; (5) Gate the legacy claimPlayer form in App.jsx 839+ for orphaned pre-S068 league_members; (6) Performance: parallelize the auth → leagues → match queries (currently sequential — visible loading time can be cut roughly in half). User picks at cold-start.

**Earlier S067 cold-start options (pre-this-session, archived):** S067 — **Production live at padel-battle.vercel.app on SW v101, main commit `a523e97`.** Cold-start direction options: (1) **Fix RulesView "undefined…" preview bug FIRST** (5 min — RulesView.jsx:23 fallback chain `(rule.subRules?.[0]?.content?.slice(0, 88) + "…")` evaluates to literal "undefined…" when chain returns undefined; fix is a ternary returning null/empty when chain is missing). (2) **PR 3 — Admin batch restyle** — AdminDashboard + SeasonManagement + LeagueManagement + PlayerManagement + PlatformAdmin all need spec port (read spec lines 1977-2173 for `AdminScreen / LeagueMgmtScreen / SeasonMgmtScreen / PlatformScreen / PlayerMgmtScreen`). Branch: `feat/46-phase12-admin-batch`. SW v101→v102. ~300-400 lines source + ~100 lines CSS, one PR fine — user already approved this scope. (3) **PR 4 — Pending/Rejected approval-gated join workflow** — DB-touching new feature for `join_requests` table + RLS + RPCs + 3 new screens (PendingApprovalScreen + RejectedScreen + ApprovalQueueScreen). Currently OnboardingScreen joins immediately on invite-code submit; spec wants admin approval first. Big scope; user-flagged as deferrable. **Standing rules (still apply across all #46 phases):** spec class names ARE the contract (Lesson #81 — class-name parity audit before visual port); push-to-main is harness-blocked → always feature-branch + `gh pr merge <N> --squash --delete-branch`; NavIcons.jsx is frozen forever; tokens use spec's LONG names; PadelLogoSmall stays as brand mark; before opening any #46 PR, run the pre-merge gate (list prior tunings via `git log --oneline -- <file>`, diff visual props spec-vs-live, classify spec-wins/prior-wins/ambiguous → ASK USER, run getComputedStyle regression checks, don't bundle architecture+visual into one PR). Deferred (no blocker): FT-15 Pairs Leaderboard (Issue #25, 6 open questions in plan); SE/DE stepper conversion; FT-07 Player Deletion Redesign needs fresh plan.

---

**Earlier session pointer (archived):** S066 — **Production live at padel-battle.vercel.app on SW v95.** Direction options for cold-start: (1) **Issue #46 Phase 8** — DB-touching: add `gender` column to `players` + capture in EditPlayerModal/EditMyProfile/onboarding, THEN add All/Men/Women filter pills + sliders icon on Players section (user-flagged via iPhone screenshot during S065 — they explicitly said "maybe it needs a different phase input first that incorporates this in the profile creation/editing section"). First DB-touching phase since FT-14 in S050. (2) **Inline log-match form restyle** inside upcoming `.scard` cards — Phase 7 explicitly out-of-scope, smaller surface than the schedule form, similar `.shi/.stog/.savebtn` vocabulary in place. (3) **Phase 9 candidate** — `LogMatch.jsx` + `EditMatchModal.jsx` paired restyle (form patterns shared). (4) **Phase 10 candidate** — Tournament screens (GameMode + BracketSVG + AmericanoMode + SE/DE/RR — big surface, intricate state). (5) **FT-15 Pairs Leaderboard** (Issue #25) — DB-touching, 6 user questions still pending in `padelhub/planning/FT-15-pairs-leaderboard.md`. User picks at cold-start. Six squash-merge commits on main: `9325a55` (Phase 1) → `4f7f693` (Phase 2) → `ac7cf35` (Phase 3) → `a909b5e` (fix overscroll + Google) → `d2cd482` (fix S058 visual restoration) → `ec31fee` (fix opaque nav + pedestal). All Vercel prod deploys READY. S061 builds **Phase 4: Leaderboard / Ranking screen redesign** — the most complex view (podium + 8-col table + form-strip + season selector + season awards). Plan needs drafting first at `padelhub/planning/issue-46-phase4-ranking.md`. Touches `App.jsx` ranking-tab block (lines ~942+) — large surface, CRLF risk (Lesson #68). Reference `padelhub/docs/PadelHub_Complete_v2.jsx` lines ~1190+ for `LeaderboardScreen` spec. Branch: `feat/46-phase4-ranking` off `main`. SW v85→v86. **MANDATORY pre-merge gate for S061+ #46 phases (per `feedback_issue46_dont_take_spec_literally.md` auto-memory):** before opening PR, (1) list every prior tuning that touched the component (`git log --oneline -- <file>`, grep for S###/Lesson/FT-NN markers); (2) diff every visual property between spec and current live; (3) classify each diff: spec-wins / prior-wins / **ambiguous → ASK USER** before applying; (4) run `getComputedStyle` checks pre-PR-open to catch regressions; (5) **don't bundle architecture migration with visual changes** — split into 2 PRs (architecture-only that reproduces visuals byte-for-byte, then visual-tweak PR with side-by-side screenshots for user review). **Other standing rules (still apply):** push-to-main is harness-blocked → always feature-branch + `gh pr merge <N> --squash --delete-branch`. NavIcons.jsx is frozen forever. Tokens use spec's LONG names. PadelLogoSmall stays as brand mark (do NOT replace with spec's `<Icon name="trophy">`). The 3-step OnboardingScreen with claim-player + DOB/gender/playing-side fields is **Phase 11** (only DB-touching phase) — don't roll it into Phase 4. Deferred (no blocker): user answers to 6 open questions in `FT-15-pairs-leaderboard.md` for #25; SE/DE stepper conversion; FT-07 Player Deletion Redesign with fresh plan.
